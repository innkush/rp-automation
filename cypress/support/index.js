

// Before each test runs
beforeEach(() => {
    // Custom setup code
  });
  
  // After each test runs
  afterEach(() => {
    // Custom teardown code
  });
  
  // Before all tests in the entire suite run
  before(() => {
    // Custom global setup code
  });
  
  // After all tests in the entire suite run
  after(() => {
    // Custom global teardown code
  });
  